global:
  log_level: info
  state:
    backend: memory

sources:
  okta-audit:
    url: "https://placeholder.okta.com/api/v1/logs"
    pagination:
      strategy: link_header
      rel: next
    resilience:
      timeout_secs: 5
